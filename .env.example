# ============================================================
# VARIABLES DE ENTORNO - SISTEMA DE RESTAURANTES
# Copia este archivo a .env y configura los valores
# ============================================================

# === BASE DE DATOS ===
DB_USERNAME=tingo_user
DB_PASSWORD=cambia-esta-contraseña-en-produccion

# === SPRING BOOT ===
SPRING_PROFILES_ACTIVE=dev

# === JWT ===
JWT_SECRET=cambia-este-secreto-por-uno-seguro-de-al-menos-256-bits-de-longitud-minima
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# === EMAIL (Gmail SMTP) ===
# Notificaciones de reserva (creada / confirmada / cancelada).
# Usa una "Contraseña de aplicación" de Gmail (NO tu contraseña normal):
#   https://myaccount.google.com/apppasswords  (requiere verificación en 2 pasos)
# Si se dejan vacías, la app funciona igual pero no envía correos.
MAIL_USERNAME=
MAIL_PASSWORD=

# === LOGIN CON GOOGLE (OAuth, rol CLIENTE) ===
# Client ID del OAuth Client (tipo "Aplicación web") creado en Google Cloud Console:
#   https://console.cloud.google.com/apis/credentials
# El MISMO valor lo usan backend (validar el token) y frontend (botón de Google).
# En "Authorized JavaScript origins" del OAuth Client agrega: http://localhost:3000
# NO es la API Key de Maps (esa empieza con "AIza..." y va aparte).
GOOGLE_CLIENT_ID=

# === ASISTENTE CON IA (Gemini - gratis) ===
# API key gratuita de Google AI Studio (https://aistudio.google.com/apikey). Vacía = modo reglas.
GEMINI_API_KEY=

# === SISTEMA DE EVENTOS (Actify) ===
# https://actify.qd.je/developers/portal — Vacía = /restaurants/near-event/{eventId} devuelve vacío.
ACTIFY_API_KEY=
ACTIFY_BASE_URL=

# === SISTEMA DE HOSPEDAJE (Hospy) ===
# https://hospy.pages.dev/ — Vacía = /restaurants/{id}/nearby-lodging devuelve vacío.
HOSPY_API_KEY=
HOSPY_BASE_URL=

# === SOFTWARE DEL MESERO (webhook piloto, un solo restaurante) ===
# Secreto compartido que envía el sistema de Meseros (../Meseros) en el header
# X-Table-Integration-Key al marcar una mesa ocupada/libre. Debe ser
# EXACTAMENTE el mismo valor en ambos sistemas. Vacía = el endpoint rechaza
# toda llamada con 403.
TABLE_ORDER_INTEGRATION_API_KEY=

# === FRONTEND ===
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Orígenes permitidos por CORS (separados por coma). Por defecto ya incluye
# localhost:3001, que es donde corre el sistema de Meseros en desarrollo.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
