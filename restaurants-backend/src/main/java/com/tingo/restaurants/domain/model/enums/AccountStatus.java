package com.tingo.restaurants.domain.model.enums;

/** Estado de la cuenta de usuario (flujo de aprobación de dueños de restaurante). */
public enum AccountStatus {
    ACTIVE,
    PENDING_REVIEW,
    REJECTED
}
